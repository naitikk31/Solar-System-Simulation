// CelestialBody implementation
// All class definitions are in the header (CelestialBody.h) since methods are inline.
// This file exists for separate compilation if needed in the future.

#include "CelestialBody.h"
